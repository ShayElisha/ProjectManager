/** Returns true if any dependency already connects these two tasks (either direction). */
export declare function tasksAlreadyLinked(taskA: string, taskB: string, deps: Array<{
    predecessorId: string;
    successorId: string;
}>): boolean;
//# sourceMappingURL=link-rules.d.ts.map