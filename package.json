{
  "name": "nexus-project",
  "version": "0.1.0",
  "private": true,
  "description": "NexusProject — Advanced project management (MS Project Premium class)",
  "dependencies": {
    "@nexus/api": "workspace:*"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "build:vercel": "bash scripts/vercel-build.sh",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.5.4",
    "typescript": "^5.8.3"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  },
  "pnpm": {
    "neverBuiltDependencies": ["mongodb-memory-server"]
  }
}
